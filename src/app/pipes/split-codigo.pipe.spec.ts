import { SplitCodigoPipe } from './split-codigo.pipe';

describe('SplitCodigoPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitCodigoPipe();
    expect(pipe).toBeTruthy();
  });
});
